# This is an example .env file that can be used to run tests locally.
# To run tests locally place this file in the thirdai_platform directory and 
# start nomad, traefik, vault, and postgres per the local setup instructions.

DB_BASE_URI="postgresql://postgres:password@localhost:5432"

PUBLIC_MODEL_BAZAAR_ENDPOINT="http://localhost:8000/"
PRIVATE_MODEL_BAZAAR_ENDPOINT="http://localhost:8000/"
NOMAD_ENDPOINT="http://localhost:4646/"

HASHICORP_VAULT_ENDPOINT="http://127.0.0.1:8200" 
HASHICORP_VAULT_TOKEN="00000000-0000-0000-0000-000000000000" 
