apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://192.168.1.8 # this should be the private ip of the node running traefik
    version: 1
    editable: false
    isDefault: true